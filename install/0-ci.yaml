install:
  - apt-get update; apt-get install -y python3.6 curl locales language-pack-en rsync unzip python3-pip
  - pip3 install -r linux_requirements 
  - eval `ssh-agent -s`; ssh-keygen; ssh-add 
script:
  - nosetests-3.4 -v -s test_instaltion.py 
