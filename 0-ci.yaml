install:
  - apt-get update; apt-get install -y python3.6 curl locales language-pack-en rsync unzip python3-pip docker
  - pip3 install -r linux_requirements 
  - eval `ssh-agent -s`; ssh-keygen -t rsa -N "" -f ~/.ssh/id_rsa ; ssh-add 
script:
  - nosetests-3.4 -v -s /opt/code/github/threefoldtech/jumpscaleX/install/tests/test_installtion.py
